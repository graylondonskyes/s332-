# SuperIDEv3 Source Lanes

This merged runtime is built from these donor lanes:

- `../SuperIDEv2`
  Frontend shell, embedded app workspace, Netlify function surface, and Dynasty product surfaces.

- `../SuperIDEv2-Sovereign-Author-Publishing-System-3.3.0-live-lane-pass (1)/build320`
  Canonical local `/api/*` server, publishing, commerce, submissions, runtime journal, and release-evidence lanes.

- `../SkyeDocxMax`
  Standalone document lane promoted into the merged public surface as `public/SkyeDocxMax`.

Notes:

- `public/SkyeDocxPro` is retained as a backward-compatible alias during the merge phase.
- The canonical completion tracker for this work remains at `../SuperIDEv3-integration.md`.
