# kAIxU-PDF-Pro Status

- Classification: `partial`
- Runnable surface: static landing page, `tool.html`, route deep-links, client-side PDF export shell, workspace export/import lane, and local PWA assets.
- Proof command: `npm run smoke:static-proof`
- What the proof covers: shell files, 18 route pages, same-origin runtime config, workspace import/export markers, client export hooks, and service-worker presence.
- What it does not cover: live gateway responses, optional vault/blob persistence, or deployed Netlify redirect behavior.
