# Apply Instructions — SkyeHands Targeted Directive V3 Theia/OpenHands Aligned

Use `ULTIMATE_IMPLEMENTATION_DIRECTIVE_SKYEHANDS_AE_GRAYCHUNKS_TARGETED_BUILDOUT_V3_THEIA_OPENHANDS_ALIGNED.md` as the active canonical directive.

This version keeps the V2 merged directive as the base and updates it to match the actual repo reality: Theia and OpenHands are not missing donor repos. Theia source already exists under `platform/ide-core/`, and OpenHands metadata/runtime boundary already exists under `platform/agent-core/`. The correct implementation target is to finish their existing install/build/import/runtime/workspace proof lanes, then make GrayChunks block any docs or UI claims that outrun `fullTheiaRuntime: true` and `fullOpenHandsRuntime: true` behavioral smoke evidence.

Do not mark implementation items complete until the related code and behavioral smoke artifacts exist. Use only `✅` and `☐` status marks.
