# V68 Deploy Guide

Deploy as normal. The valuation center will now expose the updated V68 number inside the live shell, through exported investor artifacts, and through the valuation sync lane. After deploy, open the Valuation surface and run a sync if you want the cloud health endpoint to immediately reflect the new record.
