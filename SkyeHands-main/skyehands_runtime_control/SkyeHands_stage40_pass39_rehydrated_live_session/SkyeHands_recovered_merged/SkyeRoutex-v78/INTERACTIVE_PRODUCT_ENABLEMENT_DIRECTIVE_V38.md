# INTERACTIVE_PRODUCT_ENABLEMENT_DIRECTIVE_V38

This pass stays locked on code-side enablement only.

What this directive covers:
- searchable academy index inside Routex and AE FLOW
- guided troubleshooting recipe library inside both apps
- exportable learning completion reports as HTML and JSON
- role-pack completion badge boards inside both apps
- quick-launch controls for the new enablement surfaces from the live product UI

Product intent:
The app should not force a user to guess which walkthrough or screen matters.
The product should let the user search the help system, choose a common problem, launch the exact guide that resolves it, and export learning-completion proof directly from inside the app.

Boundaries for this pass:
- code-side only
- no user-side tasks described here
- all new controls must live inside the shipped product surface
