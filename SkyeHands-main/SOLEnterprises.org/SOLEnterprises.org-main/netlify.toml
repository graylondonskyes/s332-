[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  # Use the Node runtime recommended by the project
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

