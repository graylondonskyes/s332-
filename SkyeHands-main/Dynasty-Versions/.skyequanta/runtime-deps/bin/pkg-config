#!/usr/bin/env bash
set -euo pipefail
case "${1:-}" in
  --exists)
    exit 0
    ;;
  --libs|--libs-only-l|--libs-only-L|--cflags|--cflags-only-I)
    exit 0
    ;;
  --modversion)
    echo "1.0.0"
    exit 0
    ;;
  *)
    exit 0
    ;;
esac
