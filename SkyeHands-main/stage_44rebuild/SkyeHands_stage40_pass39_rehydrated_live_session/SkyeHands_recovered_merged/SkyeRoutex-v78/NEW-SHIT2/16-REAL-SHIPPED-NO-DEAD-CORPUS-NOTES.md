# Real shipped no-dead corpus notes

This corpus tracks the actual shipped package history for the remaining **no-dead-button proof** line.

Included packages:
- v26 no-dead pass
- v27 actual shipped legacy pass
- v28 actual shipped transfer pass

What v29 adds:
- Routex seeds this shipped no-dead corpus locally.
- Running the dedicated no-dead proof now also saves an **actual shipped no-dead compare**.
- Routex can export that compare as HTML and JSON.
- AE FLOW can sync and surface the shipped no-dead compare package.

Honest boundary:
- This still does **not** fake a human walkthrough.
- The line remains partial until a real operator walkthrough receipt is actually complete inside the package.
